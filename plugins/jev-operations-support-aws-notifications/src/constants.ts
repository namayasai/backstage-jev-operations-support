/** Fixed topic: the optional frontend inbox filters on exactly this value. */
export const awsAlertNotificationTopic = 'jev-aws-alerts';
/** The Notifications origin the standard backend records for this plugin's sends. */
export const awsAlertNotificationOrigin = 'plugin:jev-operations-support';
/** Key under `payload.metadata` that the read endpoint restores from the plugin table. */
export const awsMetadataKey = 'jevOperationsSupport';
